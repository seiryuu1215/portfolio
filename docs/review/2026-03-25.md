# レビュー記録: ポートフォリオ全体の見栄え改善

**日付**: 2026-03-25
**対象コミット**: `50c1848` update: ポートフォリオ全体の見栄え改善
**対象**: 18ファイル（+273行 / -33行）

## 指摘事項と対応

### [MUST] P1: CSS変数のハードコード → 修正済み

shadow/color値が30箇所でarbitrary valueとしてハードコードされていた。
`@theme inline` で定義したCSS変数（`shadow-card`, `shadow-card-hover`, `shadow-glow-strong`, `to-accent-secondary`）を参照するように全箇所を置換。

### [SHOULD] P2: prefers-reduced-motion 未対応 → 修正済み

`globals.css` に `@media (prefers-reduced-motion: reduce)` を追加。
transition-duration/animation-duration を 0.01ms に制限。

### [INFO] P3: section-dividerが最初の3セクション間のみ

意図的に主要ブロック間（Hero-About, About-Career, Career-Works）に限定。
全セクション間に入れると過剰装飾になるため現状維持。

## 結果: 承認

P1, P2の修正完了。ビルド成功、テスト27件全パス。
